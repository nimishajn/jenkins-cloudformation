pipeline {
    agent any
    stages {
        stage('Clone Repo') {
            steps {
                sh "export AWS_DEFAULT_PROFILE=ap-southeast-2"
                sh "aws cloudformation create-stack --stack-name mys3stack --template-body file://s3bucketcreate.json --region 'ap-southeast-2'"
                }
            }
        }
    }

