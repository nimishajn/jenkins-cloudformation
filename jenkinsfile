pipeline {
    agent any
    stages {
        stage('Clone Repo') {
            steps {
                sh "export AWS_DEFAULT_PROFILE=ap-southeast-2"
                sh "aws cloudformation create-stack --stack-name myec2stack --template-body file://createEc2instance.json --region 'ap-southeast-2'"
                }
            }
        }
    }

